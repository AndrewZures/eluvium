export interface IWidth {
    full: string;
    half_screen: string;
    main_content: string;
    small: string;
    input: string;
}

export enum WidthOption {
    Small = 'small',
    Full = 'full',
    HalfScreen = 'half_screen',
    MainContent = 'main_content',
    Input = 'input',
}