import { ITextConfig } from '../types';

export const defaults: ITextConfig = {
    backgroundColor: "empty",
    color: "primary",
    fontSize: 'medium',
    height: 'input',
    lineHeight: 'small',
    width: 'input',
}